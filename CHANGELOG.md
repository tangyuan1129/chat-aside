# 更新日志

本文件只记录**旁白（chat-aside）自身**的改动。上游 Jev 聊天助手的历史记录见其
[Releases](https://github.com/jev-chat/jev-chat-jarvis/releases) 与仓库的 git 历史
（本项目从上游 v1.3 分叉，分叉点之后的历史属于本项目）。

## 0.1.1 — 2026-09-24

一轮真机回归驱动的稳定性与性能修复。

### 修复

- **[严重] 无障碍节点池泄漏**：`getChild()` 与 `rootInActiveWindow` 取出的 `AccessibilityNodeInfo`
  来自框架共享节点池，用完必须 `recycle()`。此前 9 处树遍历、11 处根节点调用点、
  `findEditable` / `findSendButton` 的返回节点全部不归还，长期运行耗尽节点池——
  表现为抓取偶发失败、分析变慢、界面卡顿（即用户反馈的「不流畅」）。现已全部回收
- **分析结果闪回空闲按钮**：会话内任何内容变化（新消息 / 正在输入 / 已读 / 滚动）都被当成
  换了会话而清空面板。改为以「包名 + 稳定标题」作为会话身份（`activeConvKey`），
  只有真正切换会话才清空
- **抖音私信气泡不弹**：首版判定过严（宽度阈值 / 强制双边），已放宽并随后按下述节点树重写

### 性能

- **内容 / 滚动事件去抖**：每个 `WINDOW_CONTENT_CHANGED` / `VIEW_SCROLLED` 事件原本都在
  主线程同步跑一次全树 DFS（守护上限 5000–6000 节点，每个 `getChild` 是一次跨进程 IPC；
  抖音/微信里光标、正在输入、动画、滚动一秒能来几十次事件）——这是界面卡顿的最大来源。
  现将一串事件合并为 150ms 后的一次采集；切 App 的窗口事件保持即时
- **分析去抖 800ms → 300ms**：采集去抖已保证快照稳定，两级原本冗余叠加；
  新消息到弹窗的感知延迟约减半

### 变更

- **抖音 / 抖音lite 适配器按真实节点树重写**（此前为无真机数据的第一版启发式）：
  - 私信输入框 `msg_et` 是抖音全屏唯一未混淆的资源 ID，且只有私信会话有——作为「在私信页」铁证
  - 真实气泡文本不带 resource-id，而页面其它文字（联系人横滑条、快捷回复、时间戳、
    系统公告、底部 tab）全部带 ID——旧几何规则会把它们当消息读进来（联系人条一行 5 条，
    正好喂饱旧的「≥4 行」误判兜底）。现在只取消息列表内的无 ID 文本
  - 无 `msg_et` 的场景（抖音lite 未验证）退回几何兜底，但页面上不存在任何无 ID 文本时
    禁用「≥4 行」弱兜底——评论列表是单列的，该形状正是误判源
- **「自动分析」默认改为关闭**：打开一个会话只亮起气泡，点「分析当前对话」才发起分析。
  付费 API 调用应由用户触发，而不是对方来一条消息就自动发生；需要自动分析可在设置里打开

### 新增

- **参谋模式新增「详细判断」区块**：面板在摘要之下逐题渲染全部 7 道判断题的概率分布
  （字面/话里有话、当前真实意图、危险等级、该不该给实质、她需要什么、最好的动作类型、
  紧张是否缓解）。这些数据本来就随每次判断一并返回，此前只渲染了压缩摘要；
  区块样式对齐上游演示图（每题一行：题面 → 选项 概率%，按可能性降序、取前 4 项）。
  聊天模型兼任判断的路线只回选项不带分布，此时显示「选项 + 把握%」
- `tools/analyze_dump.py`：uiautomator dump 解析脚本（输入框 / 滚动容器 / 文本分布），
  适配器调优时核对真实节点树用

## 0.1.0 — 2026-09-23

首次发布，从上游 v1.3 分叉。核心是**把「怎么用」和「要不要发送」的决定权交回用户**。

### 新增

- **两种可切换的呈现模式**
  - 选项模式：面板变为可点的选项串，带序号圈与推荐度，判断信息压缩为一行
  - 参谋模式：只输出判断（危险等级 / 真实意图 / 需求 / 最佳动作 / 张力是否缓解），不生成候选
- **参谋模式跳过生成阶段**：`runAnalysis()` 不再无条件并行发起生成调用，不生成候选时省掉生成模型调用与一次排序调用，单次分析成本约为三分之一
- **可配置的双击发送**（默认开启，可关闭）
  - 第一下点击填入输入框并进入待命窗口（1.5 / 2.5 / 4 秒可选）
  - 同一选项第二下才真正发送；换选项或超时即取消待命
  - 发送前校验输入框内容等于刚写入的文字，对不上则中止
  - 用户在两次点击之间手改过文字时，发送用户改动后的版本
- **按标签精确匹配的发送按钮查找**：只认 `发送 / Send` 精确标签，并优先排除含「转账、红包、收款、支付、付款、收钱、零钱、理财、银行卡」的节点；发送按钮必须位于屏幕下半部、与输入框同一行且在其右侧
- 参谋模式补上 `literal_question` 的展示（上游计算了该字段但从未呈现）
- 悬浮球菜单新增模式快捷切换；设置页新增「工作模式」卡片，改完即生效

### 变更

- Kotlin 包名 `com.jev.probe` → `io.github.tangyuan1129.chataside`
- `applicationId` / `namespace` 改为 `io.github.tangyuan1129.chataside`
- 应用名改为「旁白」，主题 `Theme.JevProbe` → `Theme.Aside`
- 无障碍服务描述改写：上游原文是「不会自动发送消息」，与本 fork 新增的双击发送能力不符，已如实描述边界
- Gradle wrapper 分发地址改为腾讯镜像（国内访问官方源超时）

### 移除

- 上游签名安装包 `apk/`、宣传站点 `site/`、赞助商与捐赠二维码等图片 `docs/images/`
- 上游内部协作说明 `CLAUDE.md`

### 保留

- `LICENSE`、`NOTICE`、`CONTRIBUTORS.md` 原样保留（MIT 要求）
- 上游设计文档 `docs/*.md`，供追溯

### 未验证

**本版本没有经过任何真机验证。** 编译通过、能产出 debug 包，但悬浮窗渲染、
双击发送、微信节点读取等行为均未在真实设备上运行过。

## 继承自上游的已知问题

以下问题随分叉一并继承，本项目未改善：

- 微信节点混淆的绕过手段随微信版本更新可能失效
- 群聊按一对一分析，「对方」判定不准
- 飞书正文依赖 OCR，可能认错字或判反说话人
- 判断模型主训练语言为英文，中文潜台词判断质量需要自行校准
- 部分国产 ROM 会冻结后台进程导致悬浮窗消失

## 许可变更记录

- 2026-09-23：本项目首次发布，沿用上游 MIT 协议，保留 LICENSE 与 NOTICE，并在
  README 中注明「基于 Jev 聊天助手二次开发」。
